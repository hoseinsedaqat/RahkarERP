## خلاصه

این Pull Request خروجی کار روی «نسخه وب» پروژه است: داشبورد واقعاً پویا، صفحه لندینگ کامل،
صفحه ورود بازطراحی‌شده، تکمیل ماژول‌های بودجه/CRM/گزارش‌گیری و زیرساخت انتشار روی GitHub Pages.

## تغییرات اصلی

### ۱. داشبورد پویا (رفع مشکل اصلی گزارش‌شده)
- محاسبه‌ی زنده‌ی همه KPIها از داده‌های واقعی: مانده نقدینگی، فروش ثبت‌شده، مطالبات جاری، سفارش‌های باز، ارزش موجودی
- «کارهای من» از رکوردها، فاکتورها، سفارش‌های خرید، تراکنش‌های خزانه، اسناد حسابداری و تیکت‌ها استخراج می‌شود
- تأیید هر کار از داشبورد، وضعیت را در ماژول مربوطه به‌روزرسانی می‌کند
- **رفع ایراد اصلی**: فرم «ثبت رویداد جدید» هندلر ذخیره نداشت و رکوردی ثبت نمی‌شد؛ اکنون ثبت، تأیید و حذف در کارتابل همه ماژول‌ها اثر فوری دارد
- کارتابل مشترک زیر صفحه‌ی هر ۱۶ ماژول، نمودار توزیع رویدادها و دسترسی سریع به ماژول‌ها

### ۲. صفحه لندینگ (طراحی ۲۰۲۶)
- هدر چسبان با منوی بخش‌ها، هیرو با پس‌زمینه‌ی متحرک و نمونه داشبورد
- ویژگی‌های محصول، ویترین ۱۶ ماژول، ۳ پلن قیمت با سوییچ ماهانه/سالانه
- بخش ارتباط با ما (فرم + اطلاعات تماس) و فوتر کامل

### ۳. صفحه ورود و خروج
- نمایش/پنهان‌سازی رمز عبور، بازگشت به صفحه اصلی، حالت بارگذاری هنگام ورود، پس‌زمینه متحرک
- دکمه خروج به صفحه ورود هدایت می‌شود

### ۴. ماژول‌های بودجه، CRM و گزارش‌گیری
- داده‌محور با افزودن/حذف سرنخ، تیکت (با تغییر وضعیت) و ردیف بودجه
- خروجی CSV واقعی برای Excel: تراز آزمایشی، فروش، خزانه، موجودی، حقوق و رویدادها

### ۵. زیرساخت وب و انتشار
- آدرس API نسبی/قابل تنظیم (`VITE_API_BASE`) و نشانگر وضعیت اتصال (متصل/آفلاین)
- سرور Node علاوه بر API خروجی `dist` را هم سرو می‌کند؛ CORS قابل تنظیم؛ اتصال روی `0.0.0.0`
- حذف `server/node-shim.d.ts` و استفاده از تایپ‌های رسمی `node:http`
- افزودن `.gitignore` و `.env.example`، حذف خروجی قدیمی `dist` از کنترل نسخه
- خروجی انتشار در `docs/` (دارای `.nojekyll`) برای GitHub Pages
- اسکریپت `scripts/publish.sh` برای انتشار یک‌دستوری

## تست و اعتبارسنجی

| بررسی | نتیجه |
| --- | --- |
| `tsc --noEmit` | بدون خطا |
| `npm run build` و `npm run build:pages` | موفق |
| تست رابط (حالت Pages/آفلاین، ۲۵ مورد) | همه موفق، بدون خطای اجرایی |
| تست حالت آنلاین با سرور (۶ مورد) | همه موفق |
| بررسی endpointها | `/`، `/api/health`، `/api/auth/login`، `/api/modules` |

موارد تست‌شده: لندینگ → ورود → داشبورد → ثبت رویداد (نمایش فوری در فید، کارهای من و کارتابل ماژول) →
تأیید کار → افزودن سرنخ CRM → تغییر دوره قیمت → ارسال فرم تماس → خروج.

## نحوه‌ی انتشار سایت

`Settings → Pages → Build and deployment`:
- Source: **Deploy from a branch** → Branch: `main` → Folder: **`/docs`**

یا با GitHub Actions (فایل workflow آماده است و با مجوز `workflows` اضافه می‌شود).

## نکته

کاربر آزمایشی: `admin` / `admin123`. بدون `DATABASE_URL` سیستم در حالت دمو اجرا می‌شود و داده‌ها در مرورگر ذخیره می‌شوند.
